import { StyleSheet } from 'react-native';
import Theme from '../../Theme';

export default StyleSheet.create({
    bar: {
        backgroundColor: Theme.colors.paleTeal10,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
    },
});
