import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    picker: {
        position: 'absolute',
        top: 0,
        left: 0,
        bottom: 0,
        right: 0,
        zIndex: 2,

        opacity: 0,
    },
});
