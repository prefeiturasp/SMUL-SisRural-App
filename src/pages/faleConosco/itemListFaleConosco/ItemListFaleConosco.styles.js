import { StyleSheet } from 'react-native';

import Theme from '../../../Theme';

export default StyleSheet.create({
    root: {
        borderLeftWidth: 1,
        borderLeftColor: Theme.colors.darkGrey,

        marginVertical: 15,
        paddingLeft: 10,
    },
});
