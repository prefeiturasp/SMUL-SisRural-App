import { StyleSheet } from 'react-native';
//import Theme from '../../../../Theme';

export default StyleSheet.create({
    options: {
        flexDirection: 'row',
        flexWrap: 'wrap',
    },
    btn: {
        flexShrink: 1,
        marginRight: 10,
        marginBottom: 10,
    },
});
